#include "gpio.ceu"
#include "wclock.ceu"
#include "adc.ceu"

output on/off PIN_13;

spawn Adc();

loop do
    await 1s;
    var int value = await Adc_Conversion(_A0);
    emit PIN_13(value > 1000);
end
